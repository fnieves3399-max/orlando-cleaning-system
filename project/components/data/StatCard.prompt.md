KPI tile for the dashboard — one metric with optional trend delta and icon chip.

```jsx
<StatCard label="Revenue this month" value="$18,240" delta="+12%" trend="up" tone="success" icon={<DollarIcon/>} />
```

`tone` tints the icon chip; `trend` up|down colors the delta green/red.
