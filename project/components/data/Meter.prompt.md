Progress / proportion bar — profit margin, goal completion, capacity.

```jsx
<Meter value={32} tone="success" label="Profit margin" valueLabel="32%" />
```

`value` 0–100; `tone` brand|success|warning|danger|navy.
