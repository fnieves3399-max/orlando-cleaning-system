Square icon-only button — toolbar actions, row overflow menus, table controls. Always pass `label` for accessibility.

```jsx
<IconButton label="Edit" variant="ghost"><PencilIcon/></IconButton>
```

Variants `secondary | ghost | primary`; sizes `sm | md | lg`.
