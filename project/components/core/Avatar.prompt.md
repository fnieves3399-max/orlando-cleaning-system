Circular avatar — customers, subcontractors, the logged-in user. Falls back to colored initials.

```jsx
<Avatar name="Maria Lopez" size="md" />
<Avatar name="Dev T" src="/uploads/dev.jpg" />
```

Sizes `xs | sm | md | lg`. Color is derived deterministically from `name`.
