Brand logo — app sidebar, login, public landing pages, slide masthead.

```jsx
<Logo variant="full" size={28} />
<Logo variant="mark" tone="light" />   {/* on navy sidebar */}
```

`variant`: `full` (mark + wordmark) or `mark` (tile only). `tone="light"` on navy/dark backgrounds.
