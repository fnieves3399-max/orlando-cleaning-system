Dropdown select — cleaning type, frequency, status, subcontractor, etc.

```jsx
<Select placeholder="Cleaning type"
  options={['Standard cleaning','Deep cleaning','Move-out cleaning','Airbnb turnover','Post-construction']} />
```

`options` accepts strings or `{value,label}`. Custom chevron, matches Input styling.
